{
  "name": "wisecar",
  "version": "1.0.0",
  "scripts": {
    "server": "json-server --watch db/db.json --port 3001 --host localhost"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}